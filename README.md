# Multi Browser Manager

A self-hosted Electron desktop app for managing many isolated browser profiles, each with its own proxy and fingerprint.

> The initial implementation lives on a feature branch. See the open PR for the full code.
